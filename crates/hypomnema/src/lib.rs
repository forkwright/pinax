//! Written record: WAL, virtual-WAL trait, and cr-sqlite-shaped causal
//! changelog feeding antigraphos (Decision 3, Decision 8, Decision 14).
//!
//! Empty scaffold reserving this crate's position in the locked dependency
//! graph (`lexis -> hypomnema -> phylaxis -> pinax`). Implementation lands
//! in Phase 02 — see `kanon/projects/pinax/ROADMAP.md`.

#![deny(missing_docs)]
