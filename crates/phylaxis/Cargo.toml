[package]
name = "phylaxis"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true
description = "(scaffold) Integrity and access guards for pinax: MVCC snapshot isolation, per-page AEAD encryption, and the heurēma index adapter."
readme = "../../README.md"
keywords = ["sql", "mvcc", "encryption"]
categories = ["data-structures"]

[dependencies]
hypomnema = { path = "../hypomnema", version = "0.0.2" }
lexis = { path = "../lexis", version = "0.0.2" }

[lints]
workspace = true

# WHY: maturity flag is read by the kanon substrate registry so consumers can
# tell at a glance which API surfaces are pre-stabilization. This crate is an
# empty scaffold reserving its position in the Decision 14 dependency graph;
# MVCC lands Phase 03, encryption lands Phase 06.
[package.metadata.kanon]
maturity = "scaffold"
since = "2026-08-15"
phase = "3"
phase-description = "empty crate reserving the workspace position and dependency edges; no implementation yet"
exit-criteria = "Phase 3 MVCC + Phase 6 encryption land; see kanon/projects/pinax/ROADMAP.md Phase 03/06"
