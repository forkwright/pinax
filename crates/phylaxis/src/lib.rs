//! Guarding: MVCC snapshot isolation, per-page AEAD encryption, and the
//! heurēma index adapter (Decision 4, Decision 9, Decision 14).
//!
//! Empty scaffold reserving this crate's position in the locked dependency
//! graph (`lexis -> hypomnema -> phylaxis -> pinax`). Implementation lands
//! in Phase 03 (MVCC) and Phase 06 (encryption) — see
//! `kanon/projects/pinax/ROADMAP.md`.

#![deny(missing_docs)]
